# Agent Guidelines for lrg-network

## Pre-commit

Before your first commit, ensure pre-commit hooks are installed by running
`make setup`. Once installed, formatting and lint checks run automatically
on every `git commit`. Never use `--no-verify` to bypass hooks.
